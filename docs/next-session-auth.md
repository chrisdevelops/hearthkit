Read CLAUDE.md, docs/STATUS.md and docs/COMPLETION-PLAN.md, then do step 5 for auth only: branch refactor/exports-auth from an up-to-date main, through the next-package loop. Stop after the PR is green and merged and STATUS is updated. Do not merge the Version Packages PR the bot opens after this changeset, and do not start payments. Delete docs/next-session-auth.md in the same PR; it is this brief and nothing else reads it.

Decisions already made for this run:

- Q1: target. Chosen B: raise the auth row in the step 5 table of docs/COMPLETION-PLAN.md to the measured allowlist (37 before any reader check, see below), as ui's row was raised to 47 in PR #32, and edit the row in the same PR. Do not drop result schemas or branded schemas to squeeze under 30.
- Q2: ./auth-contract subpath. Keep it, trimmed. It cannot be dropped: packages/payments/src/payments-contract.ts imports the AuthUserId and AuthOrganizationId types from it, and templates/app imports authApiBasePath (sign-in and account pages) and hearthkitAuthTableNames (drizzle.config.ts) from it. Follow email and ui: the subpath resolves to a new JSX-free src/auth-contract-entry.ts carrying exactly the allowlisted names that live in auth-contract.ts, plus every public type.
- Q3: CONTRACT.md rewrite. Chosen yes: rewrite from 1043 lines to under 200 in the format packages/config/CONTRACT.md uses at 96 lines and packages/email at 199. Content that is history, Better Auth research notes or gate rationale moves to docs/HISTORY.md under a dated heading "AUTH CONTRACT REWRITE (step 5)" or is dropped; only the current contract stays. The contract-author may not edit docs/HISTORY.md; it reports which sections it removed and the orchestrator moves them.

Policy from step 5: index.ts exports only the plan-named outputs, the env schema fragment, the Drizzle schema, the input schema, the output schemas, the failure union schema, and branded schemas an app must construct. Plan 4.7 names the output as "server auth instance, Next.js route handler, client hooks, session helpers, Drizzle schema for auth tables"; the 13 implementation-module values on index.ts today are those outputs. Third-party error strings, HTTP statuses, per-variant failure schemas, per-arm success schemas, options schemas, error prefixes, path suffixes, query parameter names, limits, and any constant whose only reader is a test move to test-fixtures/, or stay internal in auth-contract.ts when the implementation reads them (the email and ui contracts say which is which; copy that wording). Gate count unchanged or lower (40 today across 12 files, 8 fixture files today; add none). Every gate still passes against Postgres and the Mailpit container, the template still typechecks, its 18 gates and 6 flows still pass, payments and create gates still pass. Minor changeset for @hearthkit/auth only.

Order inside the loop: contract-author rewrites CONTRACT.md and trims auth-contract.ts first, then gate-writer moves constants into fixtures, repoints imports and adds the exact-allowlist gate, then the implementor trims index.ts, creates auth-contract-entry.ts and repoints package.json. The orchestrator decides the exact allowlist before dispatching the contract-author and writes it into the prompt by name. Reject any contract or gate set over the caps.

Facts to carry over, verified by rg on main at 0b1b186:

- Copy the pattern from packages/ui (PR #32) and packages/email (PR #30): CONTRACT.md "Package entry point" as a numbered allowlist of exactly N names, an "everything else is internal" paragraph, and a short subpath paragraph; the entry-point gate asserts sorted defined namespace keys equal a sorted as-const allowlist kept in test-fixtures, identity-equal to the contract module's values, whole-list comparison; a second assertion does the same for the subpath. See packages/ui/CONTRACT.md, packages/ui/src/index.ts, packages/ui/src/ui-contract-entry.ts, packages/ui/test-fixtures/hearthkit-ui-entry.ts, and the first gate in packages/ui/src/hearthkit-ui-exports.test.ts.
- Counts today: index.ts 113 value exports, auth-contract.ts 100, CONTRACT.md 1043 lines, 40 gates in 12 files (3,4,5,3,2,3,2,6,2,3,5,2), 8 fixture files. Count with a script over `export { ... } from` blocks and top-level export const/function lines; the scratchpad from the ui session is gone. Put before and after in the PR body for the entry and the subpath.
- The 13 implementation-module values on index.ts: resolveAuthRuntimeConfig, createAuthServerInstance, createAuthRouteHandlers, createAuthBrowserClient, readAuthSession, signUpWithPassword, signInWithPassword, requestMagicLinkSignIn, completeMagicLinkSignIn, createAuthOrganization, addAuthOrganizationMember, verifyAuthTablesExist, hearthkitAuthDrizzleSchema. All plan-named; all stay.
- Candidate contract-module values to keep public, to be checked against the contract before finalising: authEnvSchemaFragment (env fragment; templates/app/app-runtime-config.ts imports it), authFailureSchema (failure union), authRuntimeConfigSchema (the input every function takes after resolve), the ten *ResultSchema unions (resolveAuthRuntimeConfig, createAuthServerInstance, readAuthSession, signUpWithPassword, signInWithPassword, requestMagicLinkSignIn, completeMagicLinkSignIn, createAuthOrganization, addAuthOrganizationMember, verifyAuthTablesExist), authApiBasePath and hearthkitAuthTableNames (consumer-read, see Q2), and the branded schemas an app constructs: authUserIdSchema and authOrganizationIdSchema (payments brands with the types), authUserEmailSchema, authPasswordSchema, authUserNameSchema, authOrganizationNameSchema, authOrganizationSlugSchema. That is 13 + 15 + 2 + 7 = 37. Everything else in auth-contract.ts goes internal or to fixtures: nine *ErrorPrefix constants, betterAuthInvalidTokenErrorValue, betterAuthInvalidCredentialsErrorCode, betterAuthEmailAlreadyRegisteredErrorCode, betterAuthOrganizationAlreadyExistsErrorCode, betterAuthOrganizationAlreadyExistsHttpStatus, organizationRouteAbsentHttpStatus, organizationRouteUnauthorizedHttpStatus, defaultMagicLinkExpirySeconds, maximumMagicLinkExpirySeconds, minimumAuthSecretLength, minimumAuthPasswordLength, maximumAuthPasswordLength, magicLinkVerifyPathSuffix, magicLinkTokenQueryParameterName, magicLinkErrorQueryParameterName, the nine *FailureSchema variants, the twelve per-arm success schemas (authRuntimeConfigResolvedSchema, authServerInstanceCreatedSchema, authSessionActiveSchema, authSessionAbsentSchema, authSignedUpSchema, authSignedInSchema, authMagicLinkSentSchema, authOrganizationCreatedSchema, authOrganizationMemberAddedSchema, authTablesPresentSchema, authTablesMissingSchema, and any other), the twelve *OptionsSchema, authServerInstanceSchema, authRequestHeadersSchema, authDrizzleClientSchema, authRouteHandlersSchema, authBrowserClientSchema, authSessionCookieSchema, authUserSchema, authSessionSchema, socialAuthProviderConfigSchema, authInvalidFieldNameSchema, incompleteSocialProviderSchema, hearthkitAuthTableNameSchema, authEnvVariableNameSchema, authSecretSchema, authBaseUrlSchema, socialAuthProviderNameSchema, oauthClientIdSchema, oauthClientSecretSchema, authSessionIdSchema, authMemberIdSchema, magicLinkExpirySecondsSchema, authMemberRoleSchema. Check for other readers before finalising: rg -n -w each name outside packages/auth. authUserSchema and authSessionSchema may deserve to stay if the contract makes them the shape of readAuthSession's output that an app parses; decide from the contract, not from this list.
- What consumers use today: templates/app imports authEnvSchemaFragment, createAuthServerInstance, resolveAuthRuntimeConfig, readAuthSession, verifyAuthTablesExist, hearthkitAuthDrizzleSchema, createAuthRouteHandlers and the types AuthServerInstance, AuthRouteHandlers from '@hearthkit/auth'; authApiBasePath and hearthkitAuthTableNames from '@hearthkit/auth/auth-contract'. packages/payments/src/payments-contract.ts imports type AuthUserId, AuthOrganizationId from '@hearthkit/auth/auth-contract'. packages/cli and packages/create import nothing from auth at runtime; create lists it in package.json. No file under docs/ except PLAN.md and HISTORY.md names an auth export.
- Vitest aliases: packages/auth/vitest.config.ts is already the anchored array form; its ./auth-contract alias (lines 27-28) points at ./src/auth-contract.ts and must move to ./src/auth-contract-entry.ts. packages/payments/vitest.config.ts lines 39-40 and packages/create/vitest.config.ts lines 76-77 point the subpath at ../auth/src/auth-contract.ts; the gate-writer repoints both to auth-contract-entry.ts (as create's ui alias was repointed in PR #32). templates/app/vitest.config.mts has no aliases and must stay that way.
- Downstream of auth: payments (imports the subpath), templates/app, create. Delegate the gate-runner with auth, payments, create and app-template, and run those four yourself. Export the root .env first (set -a; source .env; set +a) or 8 Stripe gates in payments skip; a skipped gate is not a passing gate. The auth gates start their own Mailpit container (test-fixtures/auth-gate-mailpit-container.ts); check its ports do not collide with the flows Mailpit on 1125/8125 before running both.
- The template imports from auth, so run the Playwright flows per the appendix in docs/COMPLETION-PLAN.md. What worked for ui: an isolated Mailpit on 1125/8125, a throwaway database hk_flows_<suffix>, the hearthkit-app bucket on MinIO (it exists), an env file in the scratchpad sourced by one script that runs db:generate, migrate, build, start on PORT=3100, then SMOKE_TEST_BASE_URL=http://127.0.0.1:3100 pnpm test:e2e, then teardown in an EXIT trap. Quote EMAIL_FROM in the env file; the display-name form has angle brackets and an unquoted value is a shell parse error. The bucket script must live inside packages/storage when it runs (ESM resolves @aws-sdk/client-s3 from the script's directory); copy it in and delete it after. Expect 6 passed. Delete templates/app/drizzle afterwards and confirm git status is clean under templates/app.
- CI runs format:check over the whole repo; run pnpm format:check before pushing. Find the CI run by headSha equal to git rev-parse HEAD, and read the per-package test counts out of the log with ANSI stripped (perl -pe 's/\e\[[0-9;]*m//g'), not the exit code. Expect 12 summary lines, no skipped segment.
- Every package was bumped to 0.3.0 by Version Packages #31, merged 2026-09-10; confirm `npm view @hearthkit/auth version` says 0.3.0 before starting. Do not add changesets for templates/app. Squash-merge with --delete-branch. Commit trailers per the session's attribution reminder.
- CI on the bot's changeset-release/main branch fails at startup with zero jobs on every Version Packages PR; that is a pre-existing pattern, not a signal about your branch. Judge a release by the release run on the merge commit.
- Known but out of scope: every published tarball ships its gate files under src/ because the files field lists the whole directory. Do not fix it in this branch.
