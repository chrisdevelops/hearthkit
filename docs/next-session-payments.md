Read CLAUDE.md, docs/STATUS.md and docs/COMPLETION-PLAN.md, then do step 5 for payments only: branch refactor/exports-payments from an up-to-date main, through the next-package loop. Stop after the PR is green and merged and STATUS is updated. Do not merge the Version Packages PR the bot keeps open, and do not start step 6. Delete docs/next-session-payments.md in the same PR; it is this brief and nothing else reads it.

Decisions already made for this run:

- Q1: target. Keep the payments row in the step 5 table of docs/COMPLETION-PLAN.md at ≤ 30; the measured allowlist below is 27, so the row does not move. If a reader check below pushes it past 30, raise the row in the same PR as ui (#32) and auth (#33) did, and say so in the PR body. Do not drop result schemas or the catalog schema to squeeze under a number.
- Q2: ./payments-contract subpath. Keep it, trimmed. It cannot be dropped: packages/cli/src/cli-contract.ts imports paymentsFailureSchema and paymentsSyncedPriceSchema from it at runtime, packages/cli/src/cli-failure-results.ts and run-payments-sync-command.ts import the types PaymentsFailure, PaymentsCatalog and PaymentsEnvValues from it, packages/cli/test-fixtures/payments-sync-gate-catalog.ts imports paymentsCatalogSchema from it, and templates/app imports hearthkitPaymentsTableNames (drizzle.config.ts) and five Stripe wire constants (e2e/payments-checkout-flow.spec.ts) from it. Follow auth (#33): the subpath resolves to a new src/payments-contract-entry.ts carrying exactly the allowlisted names that live in payments-contract.ts, plus every public type. Both files must load under bare node; the cli's payments sync command relies on that.
- Q3: CONTRACT.md rewrite. Rewrite from 1304 lines to under 200 in the format packages/auth/CONTRACT.md now uses at 193 (config is 96, email 199). Everything under "Verified", "Still not verified", "Rulings and corrections", the four long argument sections ("A webhook delivery does not carry line_items", "The customer-linked path", "Deviation from plan 4.8's gate wording", "The Better Auth Stripe plugin") and "Details the gates and the implementation both depend on" moves to docs/HISTORY.md under a dated heading "PAYMENTS CONTRACT REWRITE (step 5)" or is dropped; only the current contract stays, each fact stated once. The contract-author may not edit docs/HISTORY.md; it reports which headings and line ranges it removed and the orchestrator moves them with a sed over `git show main:packages/payments/CONTRACT.md`, as was done for auth.
- Q4: the five Stripe wire constants stay public. stripeSignatureHeaderName and the four metadata keys (hearthkitBillingReferenceMetadataKey, hearthkitPriceNameMetadataKey, hearthkitQuantityMetadataKey, hearthkitStripePriceIdMetadataKey) are read by templates/app/e2e/payments-checkout-flow.spec.ts, and packages/create copies e2e/ into every scaffolded project (copy-bundled-app-template.ts excludes only src/ and test-fixtures/), so that spec is app code, not a hearthkit-only test. A project that signs a webhook body or reads Stripe metadata needs the same spelling. hearthkitBillingScopeMetadataKey has no reader outside the package and goes internal.
- Q5: the two reason enums go internal. webhookSignatureFailureReasonSchema and paymentsWebhookIgnoredReasonSchema are read only by templates/app/src/app-payments-webhook-status.test.ts (lines 215 and 281, through a dynamic import of the subpath), which is a hearthkit-only gate that create does not copy. That gate must narrow handleStripeWebhookResultSchema on kind instead, or compare the string literal. Editing that file is outside the payments gate-writer's ownership (`packages/*/src/*.test.ts`), so the orchestrator makes that one edit itself and says so in the PR body, the same way it edited the step 5 table row. Its third dynamic import, stripeSignatureHeaderName, stays valid under Q4.

Policy from step 5: index.ts exports only the plan-named outputs, the env schema fragment, the Drizzle schema, the input schema, the output schemas, the failure union schema, and branded schemas an app must construct. Plan 4.8 names the output as "createCheckoutSession, createCustomerPortalSession, handleStripeWebhook, syncPaymentsCatalog, Drizzle schema for customers, subscriptions, and purchases" and its input as the two Stripe variables plus a payments-catalog.ts file; the 9 implementation-module values on index.ts today are those outputs plus the four wrappers the contract ties to plan 4.8 gate steps. Third-party error strings, HTTP statuses, checkout modes, price types, signature scheme and tolerance, per-variant failure schemas, per-arm success schemas, options schemas, error prefixes, limits, cause-code lists, structural check schemas, and any constant whose only reader is a test move to test-fixtures/, or stay internal in payments-contract.ts when the implementation reads them (the auth contract's "Every other value ... is internal" paragraph says which is which; copy that wording). Gate count unchanged or lower (45 today across 16 files, 8 fixture files; add none). Every gate still passes with the root .env exported so no Stripe gate skips, the template still typechecks, its 18 gates and 6 flows still pass, cli and create gates still pass. Minor changeset for @hearthkit/payments only.

Order inside the loop: contract-author rewrites CONTRACT.md and trims payments-contract.ts first (un-exporting the per-arm success schemas as auth did), then gate-writer moves gate-only constants into fixtures, repoints imports, replaces the derived-list entry gate with the exact-allowlist gate and repoints the three vitest aliases, then the implementor trims index.ts, creates payments-contract-entry.ts and repoints package.json. The orchestrator decides the exact allowlist before dispatching the contract-author and writes it into the prompt by name, then makes the one template gate edit from Q5 after the implementor is done. Reject any contract or gate set over the caps.

Facts to carry over, verified by rg on main at 7498abb:

- Copy the pattern from packages/auth (PR #33): CONTRACT.md "Package entry point" as a numbered allowlist of exactly N names, an "Every other value in payments-contract.ts is internal" paragraph naming them, and a subpath paragraph; the entry-point gate asserts sorted defined namespace keys equal a sorted as-const allowlist kept in test-fixtures, identity-equal to the contract module's values, whole-list comparison, with a second assertion for the subpath. See packages/auth/CONTRACT.md, packages/auth/src/index.ts, packages/auth/src/auth-contract-entry.ts, packages/auth/test-fixtures/hearthkit-auth-entry.ts, packages/auth/test-fixtures/auth-package-entry-points.ts (runs both contract files under bare node), and the third and fourth gates in packages/auth/src/auth-env-schema-fragment.test.ts.
- Counts today: index.ts 127 value exports, payments-contract.ts 118, CONTRACT.md 1304 lines, 45 gates in 16 files (5,2,3,5,3,3,3,2,2,3,3,2,1,2,4,2), 8 fixture files. The four gates in sync-payments-catalog.test.ts are `it.skipIf(!hasGateStripeSecretKey)`; a plain `it(` count misses them and reports 37. Count with the scratchpad script pattern from the auth session: names inside `export { ... } from` blocks that are not `type`, plus top-level `export const|function` lines. Put before and after in the PR body for the entry and the subpath.
- The 9 implementation-module values on index.ts: createPaymentsClient, syncPaymentsCatalog, createCheckoutSession, createCustomerPortalSession, handleStripeWebhook, readPaymentsSubscription, listPaymentsPurchases, verifyPaymentsTablesExist, hearthkitPaymentsDrizzleSchema. All stay.
- Candidate contract-module values to keep public, to be checked against the contract before finalising: paymentsEnvSchemaFragment (templates/app/app-runtime-config.ts imports it), paymentsFailureSchema (cli-contract.ts imports it), paymentsCatalogSchema (the one input an app constructs; templates/app/payments-catalog.ts parses the catalog with it and the cli fixture does too), the eight *ResultSchema unions (createPaymentsClient, syncPaymentsCatalog, createCheckoutSession, createCustomerPortalSession, handleStripeWebhook, readPaymentsSubscription, listPaymentsPurchases, verifyPaymentsTablesExist), hearthkitPaymentsTableNames (drizzle.config.ts), paymentsSyncedPriceSchema (cli-contract.ts builds its sync result on it), and the five wire constants from Q4. That is 9 + 3 + 8 + 2 + 5 = 27. No branded schema is an input an app must construct: every string option is plain and validated inside, and the catalog is parsed by paymentsCatalogSchema; so no branded schema is on the list unless the contract-author shows one an app writes by hand. Everything else in payments-contract.ts goes internal or to fixtures: nine *ErrorPrefix constants; stripeSignatureScheme, stripeSignatureToleranceSeconds, stripeWrongSecretSignatureMessagePrefix, stripeWrongSchemeSignatureMessage, stripeParsedBodySignatureMessagePrefix, stripeUnauthorizedHttpStatus, stripeForbiddenHttpStatus, stripeSubscriptionCheckoutMode, stripeOneTimeCheckoutMode, stripeRecurringPriceType, stripeOneTimePriceType, hearthkitBillingScopeMetadataKey, handledStripeWebhookEventTypes, handledStripeWebhookEventTypeSchema, paymentsKnownSubscriptionStatuses, paymentsActiveSubscriptionStatuses, paymentsDatabaseUnavailableCauseCodes, maximumPaymentsProductNameLength, maximumPaymentsPriceNameLength, defaultCheckoutQuantity, hearthkitPaymentsTableNameSchema, stripeSecretKeySchema, stripeWebhookSecretSchema; the twenty-two branded and vocabulary schemas (billingScopeSchema, billingReferenceIdSchema, billingContactEmailSchema, paymentsProductNameSchema, paymentsPriceNameSchema, paymentsCurrencyCodeSchema, paymentsUnitAmountMinorUnitsSchema, paymentsRecurringIntervalSchema, paymentsQuantitySchema, stripeCustomerIdSchema, stripeSubscriptionIdSchema, stripeProductIdSchema, stripePriceIdSchema, stripeCheckoutSessionIdSchema, stripePaymentIntentIdSchema, stripeEventIdSchema, paymentsCustomerIdSchema, paymentsSubscriptionIdSchema, paymentsPurchaseIdSchema, paymentsSubscriptionStatusSchema, paymentsRedirectUrlSchema, paymentsStripeApiBaseUrlSchema); the catalog sub-schemas (paymentsSubscriptionCatalogPriceSchema, paymentsOneTimeCatalogPriceSchema, paymentsCatalogPriceSchema, paymentsCatalogProductSchema) and the three record schemas (paymentsCustomerSchema, paymentsSubscriptionSchema, paymentsPurchaseSchema); paymentsInvalidFieldNameSchema, paymentsCatalogIssueKindSchema, paymentsCatalogIssueSchema, priceLookupFailureSchema, webhookSignatureFailureReasonSchema, paymentsWebhookIgnoredReasonSchema, the nine per-variant *FailureSchema; paymentsClientSchema, paymentsDrizzleClientSchema, paymentsRequestHeadersSchema; the eight *OptionsSchema; paymentsPriceSyncActionSchema and paymentsWebhookOutcomeSchema; and the eleven per-arm success schemas (paymentsClientCreatedSchema, paymentsCatalogSyncedSchema, paymentsCheckoutSessionCreatedSchema, paymentsPortalSessionCreatedSchema, paymentsWebhookProcessedSchema, paymentsWebhookIgnoredSchema, paymentsSubscriptionFoundSchema, paymentsSubscriptionAbsentSchema, paymentsPurchasesListedSchema, paymentsTablesPresentSchema, paymentsTablesMissingSchema), which become module-private as auth's did. Check for other readers before finalising: rg -n -w each name outside packages/payments, excluding _.md. Every stripe_ constant is read by the implementation, so those stay internal in payments-contract.ts rather than moving to fixtures; the auth-style reader check (impl, gate, external) found no contract value whose only reader is a gate, so the fixture move may turn out to be empty. That is fine; do not invent one.
- What consumers use today: templates/app imports paymentsEnvSchemaFragment, paymentsCatalogSchema, hearthkitPaymentsDrizzleSchema, createPaymentsClient, syncPaymentsCatalog, listPaymentsPurchases, createCheckoutSession, createCustomerPortalSession, handleStripeWebhook and the types PaymentsCatalog, PaymentsClient, PaymentsPurchase, PaymentsSyncedPrice from '@hearthkit/payments'; hearthkitPaymentsTableNames plus the five wire constants from '@hearthkit/payments/payments-contract'. packages/cli imports paymentsFailureSchema and paymentsSyncedPriceSchema at runtime and the types PaymentsFailure, PaymentsCatalog, PaymentsEnvValues from the subpath, and createPaymentsClient and syncPaymentsCatalog from '@hearthkit/payments' inside run-payments-sync-command.ts through a dynamic import. packages/create imports nothing from payments at runtime. templates/app/CONTRACT.md line 199 mentions createCheckoutSessionResultSchema and handleStripeWebhookResultSchema as gate-importable; both stay public, and no template code imports them today.
- Vitest aliases: packages/payments/vitest.config.ts lines 27-28 point ./payments-contract at ./src/payments-contract.ts and must move to ./src/payments-contract-entry.ts. packages/cli/vitest.config.ts lines 30-33 and packages/create/vitest.config.ts lines 66-69 point the subpath at ../payments/src/payments-contract.ts; the gate-writer repoints both. templates/app/vitest.config.mts has no aliases and must stay that way; the template resolves the subpath through package.json exports, so its gates and flows are only correct after the implementor repoints the manifest.
- Downstream of payments: cli (imports the subpath), templates/app, create (its scaffold gates install payments). Delegate the gate-runner with payments, cli, create and app-template, and run those four yourself. Export the root .env first (set -a; source .env; set +a) or the 8 Stripe gates (4 in sync-payments-catalog, and the live gates in create-checkout-session, create-customer-portal-session and the webhook files) skip; a skipped gate is not a passing gate. The gates assert livemode is false before touching the account. There is no per-package lint script; lint runs at the root only, and the gate-runner will report that as a finding, which is not a failure.
- The template imports from payments, so run the Playwright flows per the appendix in docs/COMPLETION-PLAN.md. What worked for auth: an isolated Mailpit on 1125/8125, a throwaway database hk_flows_<suffix>, the hearthkit-app bucket on MinIO (it exists), an env file in the scratchpad sourced by one script together with the root .env that runs db:generate, migrate, build, start on PORT=3100, then SMOKE_TEST_BASE_URL=http://127.0.0.1:3100 pnpm test:e2e, then teardown in an EXIT trap. Quote EMAIL_FROM in the env file; the display-name form has angle brackets. The bucket script must live inside packages/storage when it runs (ESM resolves @aws-sdk/client-s3 from the script's directory); copy it in and delete it after. Expect 6 passed. Delete templates/app/drizzle afterwards and confirm git status is clean under templates/app.
- CI runs format:check over the whole repo; the contract-author's and gate-writer's files come back unformatted, so run `pnpm exec prettier --write` on the flagged files and `pnpm format:check` before committing. Find the CI run by headSha equal to git rev-parse HEAD, and read the per-package test counts out of the log with ANSI stripped (perl -pe 's/\e\[[0-9;]*m//g'), not the exit code. Expect 12 summary lines, no skipped segment; payments reads 45 and auth 40.
- Every package is at 0.3.0 on npm; the auth changeset from #33 is waiting in Version Packages PR #34, which stays open until the user merges it. Confirm `npm view @hearthkit/payments version` says 0.3.0 before starting, and if it says 0.4.0 the user merged #34 and nothing here changes. Do not add changesets for templates/app or cli. Squash-merge with --delete-branch. Commit trailers per the session's attribution reminder.
- CI on the bot's changeset-release/main branch fails at startup with zero jobs on every Version Packages PR; that is a pre-existing pattern, not a signal about your branch. Judge a release by the release run on the merge commit.
- Known but out of scope: every published tarball ships its gate files under src/ because the files field lists the whole directory. Do not fix it in this branch. After payments merges, step 5 is complete: tick "Export-surface refactor" in the STATUS phase checklist and point Next at step 6.
