---
'@hearthkit/ui': minor
---

New package: shadcn components (button, card, input, label, dialog, dropdown-menu), layout primitives (PageContainer, PageHeader), light/dark mode (ThemeModeProvider, ThemeModeToggle, useThemeMode), and hearthkit-theme.css defining every theme token as a CSS variable with a class-based dark variant.
