---
'@hearthkit/app-template': patch
---

The template ships `.nvmrc` with the exact Node pin `24.20.0`, its `engines` field carries the same value, and its `ci.yml` reads the version from `.nvmrc`. `appTemplateNodeVersion` in the template contract carries the value.
