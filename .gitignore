node_modules/
dist/
.next/
.reports/
.env
.env.local
*.tsbuildinfo
.DS_Store

# templates/app stores its ignore file dotless (npm strips a nested .gitignore
# from a published tarball), so its entries do not apply inside this repo.
test-results/
playwright-report/
next-env.d.ts

# packages/create bundles a copy of templates/app at prepack time so the published
# tarball is self-contained; inside the workspace the live template is used instead.
packages/create/app-template/
